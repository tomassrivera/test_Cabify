package com.android.cabifymarketplace.ui.utils

interface Constants {
    companion object {

        const val DATABASE_NAME = "cabify_marketplace_data"

        // DISCOUNT RULES
        const val DISCOUNT_RULE_MORE_THAN = "MORE THAN"
        const val DISCOUNT_RULE_QUANTITY_X_QUANTITY = "QUANTITY X QUANTITY"

        // DISCOUNT TYPES
        const val DISCOUNT_TYPE_LESS_PRICE = "LESS PRICE"
        const val DISCOUNT_TYPE_LESS_PRICE_EACH_ITEM = "LESS PRICE EACH ITEM"
        const val DISCOUNT_TYPE_PERCENTAGE = "PERCENTAGE"
    }
}